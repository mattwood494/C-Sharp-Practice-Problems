using System;

namespace Reverse_Integer
{
    /*
     * Given a 32-bit signed integer, reverse digits of an integer.

        Assumptions:
            We are dealing with an environment that could only store integers within the 32-bit signed integer range: [−2^31,  2^31 − 1]. 
            Your function returns 0 when the reversed integer overflows.

 

        Example 1:

            Input: x = 123
            Output: 321

        Example 2:

            Input: x = -123
            Output: -321

        Example 3:

            Input: x = 120
            Output: 21

        Example 4:

            Input: x = 0
            Output: 0
 

        Constraints:

        -2^31 <= x <= 2^31 - 1

    `   
            
     */
    class Program
    {
        static void Main(string[] args)
        {
            /*Pseudocode:
             * 
             * Create a method Reverse that takes an integer as input and returns an integer
             * 
             * Create an array that can hold the exact amount of integers that are inside the input
             */


        }
    }
}
